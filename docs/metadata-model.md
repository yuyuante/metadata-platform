# Metadata Model

This document reserves the domain boundary for the normalized metadata model planned for Sprint 1. The model will represent metadata objects, their source identity, ownership, attributes, and typed relationships.

Sprint 0 deliberately defines no concrete model or persistence schema. Future models should use UUID identifiers, explicit object types, source provenance, and versionable relationships.